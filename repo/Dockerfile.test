# Dedicated image for CI test runs
# node_modules are mounted from the host at runtime (no network access in this environment)
FROM node:20-slim

WORKDIR /app

# Copy all source files (node_modules excluded via .dockerignore)
COPY . .

# Default: run tests once (CI mode) using the host-mounted node_modules
CMD ["node_modules/.bin/vitest", "run"]
